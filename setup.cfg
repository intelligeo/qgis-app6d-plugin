[flake8]
# W503: line break before binary operator
# Disabled because we use the modern PEP8 style (Knuth/Black):
# operators go at the START of the continuation line (avoids W504).
extend-ignore = W503
max-line-length = 120
