line-length = 120

[lint]
select = ["E", "F", "W"]

ignore = [
    "F401",   # imported but unused ('from __future__ import annotations' is intentional)
    "E501",   # line too long (covered by line-length)
]

[lint.per-file-ignores]
"qgis_app6d/__init__.py" = ["F401", "F403"]
